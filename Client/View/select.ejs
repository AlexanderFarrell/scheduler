<select name="<%= name %>" id="<%= name %>">
    <% for (var i = 0; i < items.length; i++) { %>
        <option <%= (val === items[i]) ? "selected" : "" %>><%= items[i] %></option>
    <% } %>
</select>

